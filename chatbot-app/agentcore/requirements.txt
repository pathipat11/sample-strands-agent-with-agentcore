# Agent Core Dependencies
# Using same versions as backend for compatibility

# Core Framework
fastapi==0.137.1
uvicorn[standard]==0.49.0

# Strands Agent Framework
strands-agents[a2a,otel]>=1.30.0
strands-agents-tools[a2a_client]>=0.2.20

# OpenAI SDK - required for Bedrock Mantle OpenAI-compatible models (gpt-5.x, grok, gemma-4)
openai>=2.0.0

# Voice Chat (BidiAgent)
aws-sdk-bedrock-runtime  # Required by strands BidiNovaSonicModel

# A2A Protocol SDK for AgentCore Runtime communication
a2a-sdk>=0.3.0,<1.0.0

# AWS & Bedrock
boto3>=1.43.30

# AgentCore Memory Integration (optional, for cloud deployment only)
# Uncomment below for cloud deployment:
# bedrock-agentcore[strands-agents]>=0.1.0

# HTTP
httpx>=0.24.0
anyio>=3.6.0

# OpenTelemetry (for observability)
aws-opentelemetry-distro>=0.17.0

# Document processing
pdf2image>=1.17.0
pymupdf>=1.27.2.3  # PDF text extraction for large files exceeding Bedrock's 4.5MB limit
openpyxl>=3.1.0  # Excel sheet name extraction for preview
defusedxml>=0.7.1  # Safe XML parsing for PPTX engine

bedrock-agentcore>=1.14.1

# Nova Act SDK (for natural language browser automation via AgentCore Browser)
# NOTE: nova-act requires --no-deps due to strands-agents version conflict
# Install with: pip install nova-act==3.1.263 --no-deps
# Dependencies below are for nova-act (excluding strands-agents)
numpy<=2.4.6
pandas
pillow
pytz
retry
fire
deprecated
playwright>=1.54.0,<=1.56.0
install_playwright>=1.0.1,<2.0.0
mypy-boto3-s3

# AG-UI Protocol (agent-to-UI streaming standard)
ag-ui-protocol>=0.1.13

# Registry YAML loader
PyYAML>=6.0

# Testing
pytest>=7.0.0
pytest-asyncio>=0.21.0